#!/usr/bin/env ruby

require "gli"
require "byebug"

require_relative "../lib/tlapse"

include GLI::App
include Tlapse

program_desc "Automated time lapse photography via gphoto2"
version Tlapse::VERSION

command :doctor do |c|
  c.desc "Determine whether you are ready to start capturing"
  c.action do |global_options,options,args|
    doctor
  end
end

exit run(ARGV)

